name: demo

services:
  app:
    build: { context: ., target: dev }
    expose: ["8080"]               # the port the app listens on; houston dev serves it at http://demo.localhost
    volumes:
      - .:/app

x-houston:
  health: /
  # app_port: 8080                # production's port, when the production image listens elsewhere
  # domains: [example.com]
  # deploy: { on: commit, branch: main }
  # commands:
  #   console: sh
  #   test: "true"
  # hooks:
  #   release: ./migrate
  # backups: { schedule: "daily 03:00", keep: { auto: 14, deploy: 10 } }
